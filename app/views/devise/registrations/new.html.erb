<h2>Sign up</h2>
<div class="row">
  <div class="col-md-6 col col-md-offset-3 text-center">
    <% if params[:plan] == '2' %>
      <h3>Pro Account</h3>
      <p>Already leveraging our sms services, get to all the info you need.</p>
    <% else %>
      <h3>Basic Account</h3>
      <p>Sign up for free and see what the mars sms team can do for you!</p>
    <% end %>
  </div>  
  <div class="col-md-4 col-md-offset-4">
    <div class="well">
      <% if params[:plan] == '2' %>
        <%= render "pro_form" %>
      <% else %>
        <%= render "basic_form" %>
      <% end %>
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>

